"use client"; import {useState} from "react"; import {auth} from "@/lib/api"; import {Alert} from "@/components/ui";
export default function Page(){const [message,setMessage]=useState(""),[busy,setBusy]=useState(false);return <div className="card p-7 text-center"><div className="mx-auto grid h-14 w-14 place-items-center rounded-full bg-green-100 text-2xl">✉</div><h1 className="mt-5 text-2xl font-bold">Verify your email</h1><p className="muted mt-3">Open the verification link we sent you. Access to workspace data remains locked until your address is verified.</p>{message&&<div className="mt-4"><Alert>{message}</Alert></div>}<button className="btn btn-primary mt-6" disabled={busy} onClick={async()=>{setBusy(true);try{setMessage((await auth.resend()).message)}catch{setMessage("We couldn’t resend the email. Please try again.")}finally{setBusy(false)}}}>Resend verification email</button></div>}
