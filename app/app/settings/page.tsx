import {PageTitle} from "@/components/ui";export default function Page(){return <><PageTitle title="Workspace settings" detail="Configuration for your current workspace."/><div className="grid gap-5 md:grid-cols-2"><section className="card p-6"><h2 className="font-bold">Workspace</h2><p className="muted mt-2 text-sm">Workspace identity is available read-only through the current-business endpoint. A safe update endpoint is not currently exposed.</p></section><section className="card p-6"><h2 className="font-bold">Email acknowledgement</h2><p className="muted mt-2 text-sm">Acknowledgement configuration exists in the backend domain, but no public tenant settings contract is available. Controls remain hidden to avoid unsafe generic writes.</p></section></div></>}
