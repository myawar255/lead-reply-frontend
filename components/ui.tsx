import Link from "next/link";
export function Spinner(){return <div role="status" className="p-8 text-center muted">Loading…</div>}
export function Empty({title,detail}:{title:string;detail:string}){return <div className="card p-10 text-center"><h3 className="text-lg font-bold">{title}</h3><p className="muted mt-2">{detail}</p></div>}
export function Alert({children}:{children:React.ReactNode}){return <div role="alert" className="rounded-xl border border-red-200 bg-red-50 p-3 text-sm text-red-800">{children}</div>}
export function PageTitle({title,detail,action}:{title:string;detail?:string;action?:React.ReactNode}){return <div className="mb-6 flex flex-wrap items-end justify-between gap-4"><div><h1 className="text-2xl font-bold">{title}</h1>{detail&&<p className="muted mt-1">{detail}</p>}</div>{action}</div>}
export function Pager({meta,base,query}:{meta:{current_page:number;last_page:number};base:string;query:URLSearchParams}){const link=(p:number)=>{const q=new URLSearchParams(query);q.set("page",String(p));return `${base}?${q}`};return <nav aria-label="Pagination" className="mt-5 flex items-center justify-between text-sm"><Link className={`btn btn-secondary ${meta.current_page<=1?"pointer-events-none opacity-50":""}`} href={link(meta.current_page-1)}>Previous</Link><span>Page {meta.current_page} of {meta.last_page}</span><Link className={`btn btn-secondary ${meta.current_page>=meta.last_page?"pointer-events-none opacity-50":""}`} href={link(meta.current_page+1)}>Next</Link></nav>}
